<h1>Add a dose to <%= @cocktail.name %> cocktail!</h1>

<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.input :description %>
  <%# f.association :ingredient %>

  <%= f.input :ingredient_id, collection: @ingredients %>

  <%= f.button :submit %>
<% end %>
